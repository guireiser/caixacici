name = "caixa-cici-worker"
main = "index.js"
compatibility_date = "2026-03-11"

# Segredos e configuracao sensivel NAO ficam neste arquivo (repositorio publico).
# Producao: wrangler secret put JSONBIN_BIN_ID | JSONBIN_KEY | ADMIN_PASSWORD_HASH
# Opcional: wrangler secret put ALLOWED_ORIGINS
# Local: copie .dev.vars.example para .dev.vars e preencha (gitignore).
