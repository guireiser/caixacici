{
  "name": "caixa-cici",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:lan": "node scripts/write-config-lan.js && concurrently -n worker,vite -c blue,green \"npm run dev:lan:worker\" \"npm run dev:lan:vite\"",
    "dev:lan:worker": "cd worker && npx wrangler dev --ip 0.0.0.0",
    "dev:lan:vite": "vite --host",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite": "^7.3.1"
  }
}
